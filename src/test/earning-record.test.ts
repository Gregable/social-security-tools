// This file intentionally left with no active tests.
// Original EarningRecord tests were merged into recipient.test.ts.
// We keep this placeholder (rather than delete) because deletion attempts
// have not taken effect in the current environment session. To avoid
// duplicate execution, we export an empty (skipped) suite.

import { describe } from 'vitest';

describe.skip('EarningRecord (migrated)', () => {});
